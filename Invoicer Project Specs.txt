* Client Database
   * Storing data about clients
      * Business name
      * Which doctor services them
         * There are three doctors
      * Name+email address of various persons(admin, billing officer, etc)
      * Expected monthly billing amount
* Invoice Generation
   * Client
   * Service Date
   * Billing Date(when invoice sent out)
   * Service + amount(autofill from client but allow editing)
   * Save it to PDF
      * Make sure it has a certain format
      * todo: figure out invoice format
   * Shortcut: Select all clients of a doctor and make a new invoice for each of their clients(with updated billing/service date)
* Billing Records
   * Sent Invoice
   * Payment status
   * Check data
      * All information on the check
         * Except business address
   * Don't need to save data about when check was received etc
   * Check might have date info save that
* E-Mail Integration(Prospective)
   * Put this in the invoice generator section maybe
* Add a button to play the game simon(colored buttons one) in any section
* AI check stub recognition through a phone app(Very Prospective)
   * Take a picture of the check stub and then the ai figures it all out and updates the billing records
   * Fill in the details once we get here
   * Prompt
      * Create a data(as an output)
         * Manipulate the data answer